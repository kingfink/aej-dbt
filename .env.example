# Store only this value in the Modal Secret named `aej-dbt-bq`.
SERVICE_ACCOUNT_JSON={"type":"service_account", "...":"..."}

# Store only this value in the Modal Secret named `aej-dbt-healthchecks`.
HEALTHCHECKS_PING_URL=https://hc-ping.com/<check-uuid>
