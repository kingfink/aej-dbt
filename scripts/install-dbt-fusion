#!/usr/bin/env bash
set -euo pipefail

install_dir="${1:-/usr/local/bin}"
export SHELL="${SHELL:-/bin/bash}"

curl -fsSL https://public.cdn.getdbt.com/fs/install/install.sh \
  | sh -s -- --version stable --to "$install_dir"
