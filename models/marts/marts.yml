version: 2

models:
  - name: dim_jobs
    description: One row per organization and job slug for every job published on Analytics Engineering Jobs.
    config:
      group: site_content
    columns:
      - name: job_id
        data_type: string
        description: Stable surrogate key generated from the organization and job slugs.
        constraints:
          - type: not_null
        tests:
          - not_null
          - unique
      - name: organization_id
        data_type: string
        description: Organization dimension key for the organization that published the job.
        constraints:
          - type: not_null
        tests:
          - not_null
          - relationships:
              arguments:
                to: ref("dim_organizations")
                field: organization_id
      - name: organization_slug
        data_type: string
        description: Slug identifying the organization that published the job.
        constraints:
          - type: not_null
        tests:
          - not_null
      - name: job_slug
        data_type: string
        description: Slug identifying the job within its organization.
        constraints:
          - type: not_null
        tests:
          - not_null
      - name: is_active
        data_type: bool
        description: Whether the job is currently active on the site.
        tests:
          - accepted_values:
              arguments:
                values:
                  - true
                  - false
                quote: false
      - name: is_featured
        data_type: bool
        description: Whether the job is featured on the site.
        tests:
          - accepted_values:
              arguments:
                values:
                  - true
                  - false
                quote: false
      - name: date_added
        data_type: date
        description: Date the job was added to the site.
      - name: date_removed
        data_type: date
        description: Date the job was removed from the site, when applicable.
      - name: title
        data_type: string
        description: Published job title.
      - name: posting_url
        data_type: string
        description: URL of the external job posting.
      - name: description
        data_type: string
        description: Published job description.
      - name: location
        data_type: string
        description: Published job location.
      - name: salary
        data_type: string
        description: Published salary information.
      - name: tags
        data_type: array<string>
        description: Tags assigned to the job.

  - name: dim_organizations
    description: One row per organization published on Analytics Engineering Jobs.
    config:
      group: site_content
    columns:
      - name: organization_id
        data_type: string
        description: Stable surrogate key generated from the organization slug.
        constraints:
          - type: not_null
        tests:
          - not_null
          - unique
      - name: organization_slug
        data_type: string
        description: Slug identifying the organization.
        constraints:
          - type: not_null
        tests:
          - not_null
          - unique
      - name: is_active
        data_type: bool
        description: Whether the organization is currently active on the site.
        tests:
          - accepted_values:
              arguments:
                values:
                  - true
                  - false
                quote: false
      - name: is_featured
        data_type: bool
        description: Whether the organization is featured on the site.
        tests:
          - accepted_values:
              arguments:
                values:
                  - true
                  - false
                quote: false
      - name: name
        data_type: string
        description: Published organization name.
      - name: description
        data_type: string
        description: Published organization description.
      - name: description_short
        data_type: string
        description: Short published organization description.
      - name: organization_url
        data_type: string
        description: Published organization website URL.
      - name: headcount
        data_type: string
        description: Published or inferred organization headcount range.
      - name: location
        data_type: string
        description: Published or inferred organization location.

  - name: dim_web_sessions
    description: |
      One row per PostHog session captured on the production site, carrying the session entry attribution and the acquisition channel derived from it. PostHog stamps the entry referrer and UTM properties on every event in a session and they are constant within a session, so the grain is a plain aggregation over the session identifier and no entry event selection rule is needed. PostHog is consent-gated, so sessions here are a floor on real traffic rather than total traffic.
    config:
      group: web_engagement
    columns:
      - name: web_session_id
        data_type: string
        description: Stable surrogate key generated from the source and the source session identifier.
        constraints:
          - type: not_null
        tests:
          - not_null
          - unique
      - name: source_session_id
        data_type: string
        description: Session identifier supplied by the behavioral analytics provider.
        constraints:
          - type: not_null
        tests:
          - not_null
          - unique
      - name: source
        data_type: string
        description: Behavioral analytics provider that captured the session.
        constraints:
          - type: not_null
        tests:
          - not_null
          - accepted_values:
              arguments:
                values:
                  - posthog
      - name: visitor_id
        data_type: string
        description: |
          Anonymous device identifier for the visitor. Person profiles are disabled, so a visitor is not reconciled across devices or browsers and repeat visitors are undercounted.
        constraints:
          - type: not_null
        tests:
          - not_null
      - name: session_started_ts
        data_type: timestamp
        description: Timestamp of the earliest captured event in the session, in UTC.
        constraints:
          - type: not_null
        tests:
          - not_null
      - name: session_entry_page_path
        data_type: string
        description: Path of the first page of the session.
      - name: session_entry_referring_domain
        data_type: string
        description: Host the session was referred from, or `$direct` when the visitor arrived with no referrer.
      - name: session_entry_utm_source
        data_type: string
        description: Lowercased campaign source on the session entry URL.
      - name: session_entry_utm_medium
        data_type: string
        description: Lowercased campaign medium on the session entry URL.
      - name: session_entry_utm_campaign
        data_type: string
        description: Campaign name on the session entry URL.
      - name: channel
        data_type: string
        description: |
          Acquisition channel of the session, evaluated in order with the first match winning: UTM medium `email` or UTM source `newsletter` is `email`; UTM medium `paid`, `cpc`, or `ppc` is `paid`; UTM medium `organic` is `organic_search`; UTM medium `referral` is `referral`; a `$direct` referring domain is `direct`; the site's own domain is `internal`; a known AI assistant host is `ai_assistant`; a known search engine host is `organic_search`; a known webmail host is `email`; anything else is `referral`.

          UTM deliberately beats the referring domain, because email clients strip the referrer and newsletter traffic otherwise reads as direct.

          `internal` is not direct traffic. It is a session whose entry referrer is the site's own domain, which happens when the 30-minute session window expires mid-visit, and it accounted for a quarter of sessions over the 90 days to 2026-09-11. A fact row with no session context has a null `web_session_id` and therefore no channel at all, which is also not direct.
        constraints:
          - type: not_null
        tests:
          - not_null
          - accepted_values:
              arguments:
                values:
                  - ai_assistant
                  - direct
                  - email
                  - internal
                  - organic_search
                  - paid
                  - referral

  - name: fct_form_submissions
    description: One row per legacy Webflow or current Netlify form submission, linked to known subscribers and site-content dimensions when matching records exist.
    config:
      group: web_engagement
    columns:
      - name: form_submission_id
        data_type: string
        description: Stable surrogate key generated from the source and source submission identifier.
        constraints:
          - type: not_null
        tests:
          - not_null
          - unique
      - name: source_form_submission_id
        data_type: string
        description: Submission identifier supplied by the source platform.
        constraints:
          - type: not_null
        tests:
          - not_null
      - name: source
        data_type: string
        description: Platform that collected the form submission.
        constraints:
          - type: not_null
        tests:
          - not_null
          - accepted_values:
              arguments:
                values:
                  - netlify
                  - webflow
      - name: form_name
        data_type: string
        description: Canonical form name, with deprecated Webflow variants consolidated under newsletter.
        constraints:
          - type: not_null
        tests:
          - not_null
          - accepted_values:
              arguments:
                values:
                  - job-apply-capture
                  - newsletter
                  - offer-review
                  - resume-review
      - name: subscriber_id
        data_type: string
        description: Subscriber dimension key when the submitted address has a known email subscription lifecycle.
        tests:
          - relationships:
              arguments:
                to: ref("dim_email_subscribers")
                field: subscriber_id
      - name: job_id
        data_type: string
        description: Job dimension key for a job-apply capture submission with matching job context.
        tests:
          - relationships:
              arguments:
                to: ref("dim_jobs")
                field: job_id
      - name: organization_id
        data_type: string
        description: Organization dimension key when the submission contains matching organization context.
        tests:
          - relationships:
              arguments:
                to: ref("dim_organizations")
                field: organization_id
      - name: email_address
        data_type: string
        description: Lowercased and trimmed submitted email address.
        constraints:
          - type: not_null
        tests:
          - not_null
          - is_normalized_email_address
      - name: web_session_id
        data_type: string
        description: |
          Web session dimension key when the submission carries a session identifier that matches a captured PostHog session. Null means no session context rather than direct traffic: `posthog_session_id` capture on Netlify forms began in 2026-07 and is complete from 2026-08 onward, and visitors who declined analytics consent never carry one.
        tests:
          - relationships:
              arguments:
                to: ref("dim_web_sessions")
                field: web_session_id
      - name: posthog_distinct_id
        data_type: string
        description: Anonymous PostHog browser identifier when the visitor explicitly enabled PostHog analytics, retained as a nullable join key to behavioral events.
      - name: posthog_session_id
        data_type: string
        description: PostHog session identifier when the visitor explicitly enabled PostHog analytics, retained as a nullable join key to behavioral events.
      - name: form_submission_details
        data_type: json
        description: Native JSON object containing populated analytics context not represented by a dimension key, currently job title and outbound job URL.
        constraints:
          - type: not_null
        tests:
          - not_null
      - name: submitted_ts
        data_type: timestamp
        description: Timestamp when the source received the form submission.
        constraints:
          - type: not_null
        tests:
          - not_null

  - name: fct_web_events
    description: |
      One row per job page view and outbound apply click captured on the production site. `stg_posthog__events` retains every captured event type, so widening this fact is a one-line change. Events are attributed to a job and organization when the event carries job context, either as event properties or as a job page path. Page views are only captured for visitors who consented to analytics, so counts are a floor rather than total traffic. History begins 2025-12-22, and apply clicks begin 2026-04-20.
    config:
      group: web_engagement
    columns:
      - name: event_id
        data_type: string
        description: Stable surrogate key generated from the source and the source event ID.
        constraints:
          - type: not_null
        tests:
          - not_null
          - unique
      - name: source_event_id
        data_type: string
        description: Event identifier supplied by the behavioral analytics provider.
        constraints:
          - type: not_null
        tests:
          - not_null
          - unique
      - name: source
        data_type: string
        description: Behavioral analytics provider that captured the event.
        constraints:
          - type: not_null
        tests:
          - not_null
          - accepted_values:
              arguments:
                values:
                  - posthog
      - name: event_type
        data_type: string
        description: |
          Provider-independent event type. `apply_click` fires on the outbound application link itself and is independent of what follows in the apply email-capture flow.
        constraints:
          - type: not_null
        tests:
          - not_null
          - accepted_values:
              arguments:
                values:
                  - apply_click
                  - page_view
      - name: event_ts
        data_type: timestamp
        description: Timestamp when the event occurred, in UTC.
        constraints:
          - type: not_null
        tests:
          - not_null
      - name: visitor_id
        data_type: string
        description: |
          Anonymous device identifier for the visitor. Person profiles are disabled, so a visitor is not reconciled across devices or browsers and repeat visitors are undercounted.
        constraints:
          - type: not_null
        tests:
          - not_null
      - name: web_session_id
        data_type: string
        description: Web session dimension key for the session the event belongs to, carrying the session entry attribution and acquisition channel.
        tests:
          - relationships:
              arguments:
                to: ref("dim_web_sessions")
                field: web_session_id
      - name: session_id
        data_type: string
        description: Session identifier the event belongs to.
      - name: page_path
        data_type: string
        description: Path of the page the event occurred on.
      - name: job_id
        data_type: string
        description: Job dimension key when the event carries job context.
        tests:
          - relationships:
              arguments:
                to: ref("dim_jobs")
                field: job_id
      - name: organization_id
        data_type: string
        description: Organization dimension key when the event carries organization context.
        tests:
          - relationships:
              arguments:
                to: ref("dim_organizations")
                field: organization_id
      - name: event_details
        data_type: json
        description: |
          Native JSON object containing populated event-specific details such as outbound_url, link_text, and click_method.
        constraints:
          - type: not_null
        tests:
          - not_null

  - name: dim_email_campaigns
    description: |
      One row per email campaign, where a campaign is the batch of messages sharing a source, subject, and campaign date. Transactional and test sends are campaigns too; `is_test` separates the test sends and the subject separates the weekly newsletter from transactional mail.
    config:
      group: email
    columns:
      - name: email_campaign_id
        data_type: string
        description: Stable surrogate key generated from the source, subject, and campaign date.
        constraints:
          - type: not_null
        tests:
          - not_null
          - unique
      - name: source
        data_type: string
        description: Email provider that sent the campaign.
        constraints:
          - type: not_null
        tests:
          - not_null
          - accepted_values:
              arguments:
                values:
                  - resend
                  - sendgrid
      - name: campaign_name
        data_type: string
        description: |
          Campaign name taken from the `utm_campaign` parameter on the campaign's tracked links, such as `weekly_jobs_2026_08_11`. Null for campaigns whose links carry no campaign parameter and for campaigns that were never clicked.
      - name: subject
        data_type: string
        description: Campaign subject line when supplied by the provider. Always null for SendGrid, which did not record subjects.
      - name: campaign_date
        data_type: date
        description: |
          UTC date the campaign was sent. Anchored to the earliest event on the campaign's messages when the provider never delivered a sent event.
        constraints:
          - type: not_null
        tests:
          - not_null
      - name: first_sent_ts
        data_type: timestamp
        description: Earliest sent-event timestamp across the campaign's messages. Null when no message in the campaign ever recorded a sent event.
      - name: message_count
        data_type: int64
        description: Number of provider messages in the campaign, which is one per recipient send.
        constraints:
          - type: not_null
        tests:
          - not_null
      - name: is_test
        data_type: bool
        description: Whether the campaign is a test send, identified by a bracketed TEST subject prefix.
        constraints:
          - type: not_null
        tests:
          - not_null
          - accepted_values:
              arguments:
                values:
                  - true
                  - false
                quote: false

  - name: dim_email_messages
    description: One row per provider email message across SendGrid and Resend.
    config:
      group: email
    columns:
      - name: email_id
        data_type: string
        description: Stable surrogate key generated from the source and source email ID.
        constraints:
          - type: not_null
        tests:
          - not_null
          - unique
      - name: email_campaign_id
        data_type: string
        description: Campaign dimension key for the campaign the message was part of.
        constraints:
          - type: not_null
        tests:
          - not_null
          - relationships:
              arguments:
                to: ref("dim_email_campaigns")
                field: email_campaign_id
      - name: source
        data_type: string
        description: Email provider that sent the message.
        constraints:
          - type: not_null
        tests:
          - not_null
          - accepted_values:
              arguments:
                values:
                  - resend
                  - sendgrid
      - name: source_email_id
        data_type: string
        description: Message identifier supplied by the email provider.
        constraints:
          - type: not_null
        tests:
          - not_null
      - name: subject
        data_type: string
        description: Email subject line when supplied by the provider.
      - name: sent_ts
        data_type: timestamp
        description: Earliest known sent-event timestamp for the message.

  - name: dim_email_subscribers
    description: |
      One row per normalized email address with a subscription lifecycle event. The current `is_subscribed` value reflects Resend contact state; SendGrid events contribute historical context but do not establish current subscription status.
    config:
      group: email
    columns:
      - name: subscriber_id
        data_type: string
        description: Stable surrogate key generated from the normalized email address.
        constraints:
          - type: not_null
        tests:
          - not_null
          - unique
      - name: email_address
        data_type: string
        description: Lowercased and trimmed subscriber email address.
        constraints:
          - type: not_null
        tests:
          - not_null
          - unique
          - is_normalized_email_address
      - name: resend_contact_id
        data_type: string
        description: Resend contact identifier when the subscriber is known to Resend.
      - name: is_subscribed
        data_type: bool
        description: Whether the address is currently a subscribed Resend contact.
        constraints:
          - type: not_null
        tests:
          - not_null
      - name: min_subscribed_ts
        data_type: timestamp
        description: Earliest known subscription timestamp.
      - name: max_subscribed_ts
        data_type: timestamp
        description: Most recent known subscription or resubscription timestamp.
      - name: min_unsubscribed_ts
        data_type: timestamp
        description: Earliest timestamp at which an unsubscribed state is known; snapshot-derived values use the backfill timestamp and do not identify the precise unsubscribe time.
      - name: max_unsubscribed_ts
        data_type: timestamp
        description: Most recent timestamp at which an unsubscribed state is known; snapshot-derived values use the backfill timestamp and do not identify the precise unsubscribe time.

  - name: fct_email_events
    description: |
      One row per provider email event and recipient across SendGrid and Resend, including synthetic sent events from the Resend backfill when no matching webhook event exists. Click events additionally carry a normalized link destination, so link-level analysis needs no separate fact.
    config:
      group: email
    columns:
      - name: email_event_id
        data_type: string
        description: Stable identifier for the provider event and recipient.
        constraints:
          - type: not_null
        tests:
          - not_null
          - unique
      - name: subscriber_id
        data_type: string
        description: Subscriber dimension key when the event recipient has a known subscription lifecycle.
        tests:
          - relationships:
              arguments:
                to: ref("dim_email_subscribers")
                field: subscriber_id
      - name: email_id
        data_type: string
        description: Message dimension key for the event.
        constraints:
          - type: not_null
        tests:
          - not_null
          - relationships:
              arguments:
                to: ref("dim_email_messages")
                field: email_id
      - name: event_type
        data_type: string
        description: Provider-independent event type such as sent, delivered, opened, or clicked.
        constraints:
          - type: not_null
        tests:
          - not_null
          - accepted_values:
              arguments:
                values:
                  - bounced
                  - clicked
                  - delivered
                  - delivery_delayed
                  - opened
                  - resubscribed
                  - sent
                  - suppressed
      - name: event_ts
        data_type: timestamp
        description: Timestamp when the event occurred, in UTC.
        constraints:
          - type: not_null
        tests:
          - not_null
      - name: link_url
        data_type: string
        description: |
          Clicked destination URL with the query string and fragment removed, so campaign parameters and per-contact unsubscribe tokens collapse to one value per destination. Null for events that are not clicks.
      - name: link_type
        data_type: string
        description: |
          Destination category of the clicked link. `job_page` is a job posting on the site, `site` is any other page on the site, `unsubscribe` is a provider-hosted unsubscribe page, and `external` is any other destination. Null for events that are not clicks.
        tests:
          - accepted_values:
              arguments:
                values:
                  - external
                  - job_page
                  - site
                  - unsubscribe
      - name: job_id
        data_type: string
        description: |
          Job dimension key when the clicked link points at a job posting under the current job URL scheme. Legacy SendGrid-era job links used a flat single-segment path and stay unresolved.
        tests:
          - relationships:
              arguments:
                to: ref("dim_jobs")
                field: job_id
      - name: is_engagement
        data_type: bool
        description: Whether the event is an open or click. Privacy-proxied and bot-like activity remains included.
        constraints:
          - type: not_null
        tests:
          - not_null
          - accepted_values:
              arguments:
                values:
                  - true
                  - false
                quote: false
      - name: is_privacy_or_bot_like
        data_type: bool
        description: Whether the event details classify the open or click as privacy-proxied or bot-like.
        constraints:
          - type: not_null
        tests:
          - not_null
          - accepted_values:
              arguments:
                values:
                  - true
                  - false
                quote: false
      - name: email_event_details
        data_type: json
        description: |
          Native JSON object containing populated event-specific details such as ip_address, user_agent, is_backfill, is_privacy_or_bot_like, and privacy_or_bot_reason. The clicked link is promoted to the `link_url`, `link_type`, and `job_id` columns.
        constraints:
          - type: not_null
        tests:
          - not_null

  - name: fct_email_subscribers_daily
    description: One row per UTC date and subscriber from the subscriber's first lifecycle event through the current date, indicating whether the subscriber was subscribed at any point during the date. SendGrid state is used through the Resend contact-import date; Resend state is authoritative beginning the following date because the migration imported both subscribed and unsubscribed contact state. Backfilled unsubscribes represent the state known at migration rather than a precise historical unsubscribe date.
    config:
      group: email
    columns:
      - name: email_subscriber_daily_id
        data_type: string
        description: Stable surrogate key generated from the date and subscriber.
        constraints:
          - type: not_null
        tests:
          - not_null
          - unique
      - name: date_day
        data_type: date
        description: UTC date of the daily subscription state.
        constraints:
          - type: not_null
        tests:
          - not_null
      - name: subscriber_id
        data_type: string
        description: Subscriber dimension key.
        constraints:
          - type: not_null
        tests:
          - not_null
          - relationships:
              arguments:
                to: ref("dim_email_subscribers")
                field: subscriber_id
      - name: is_subscribed
        data_type: bool
        description: Whether the subscriber entered the date subscribed or had a Resend subscribe event during the date.
        constraints:
          - type: not_null
        tests:
          - not_null
          - accepted_values:
              arguments:
                values:
                  - true
                  - false
                quote: false

  - name: fct_email_subscriber_messages
    description: One row per email message and known subscriber, with repeated opens and clicks retained in event counts. Event recipients without a known subscription lifecycle remain in `fct_email_events` but are outside this subscriber-level fact. All message-level metrics use min_sent_ts as their time basis.
    config:
      group: email
    columns:
      - name: email_subscriber_message_id
        data_type: string
        description: Stable surrogate key generated from the email and subscriber keys.
        constraints:
          - type: not_null
        tests:
          - not_null
          - unique
      - name: email_id
        data_type: string
        description: Email message dimension key.
        constraints:
          - type: not_null
        tests:
          - not_null
          - relationships:
              arguments:
                to: ref("dim_email_messages")
                field: email_id
      - name: subscriber_id
        data_type: string
        description: Subscriber dimension key.
        constraints:
          - type: not_null
        tests:
          - not_null
          - relationships:
              arguments:
                to: ref("dim_email_subscribers")
                field: subscriber_id
      - name: min_sent_ts
        data_type: timestamp
        description: Earliest sent-event timestamp for the message and subscriber; the time basis for all message-level metrics.
      - name: min_delivered_ts
        data_type: timestamp
        description: Earliest delivered-event timestamp for the message and subscriber.
      - name: min_opened_ts
        data_type: timestamp
        description: Earliest open-event timestamp, including privacy-proxied and bot-like activity.
      - name: min_clicked_ts
        data_type: timestamp
        description: Earliest click-event timestamp, including privacy-proxied and bot-like activity.
      - name: n_events_opened
        data_type: int64
        description: Number of open events, including repeated, privacy-proxied, and bot-like activity.
        constraints:
          - type: not_null
        tests:
          - not_null
      - name: n_events_clicked
        data_type: int64
        description: Number of click events, including repeated, privacy-proxied, and bot-like activity.
        constraints:
          - type: not_null
        tests:
          - not_null

unit_tests:
  - name: dim_web_sessions_derives_channel_precedence
    description: |
      UTM beats a `$direct` referring domain, an AI assistant host beats the search engine it is hosted under, a webmail host beats the search engine it is hosted under, the site's own domain is internal rather than referral, and an event with no session identifier produces no session row.
    model: dim_web_sessions
    given:
      - input: ref("stg_posthog__events")
        rows:
          - {session_id: newsletter, visitor_id: device_newsletter, event_ts: "2026-09-01 00:00:00+00", session_entry_page_path: /, session_entry_referring_domain: $direct, session_entry_utm_source: Newsletter, session_entry_utm_medium: Email, session_entry_utm_campaign: null}
          - {session_id: newsletter, visitor_id: device_newsletter, event_ts: "2026-09-01 00:30:00+00", session_entry_page_path: /, session_entry_referring_domain: $direct, session_entry_utm_source: Newsletter, session_entry_utm_medium: Email, session_entry_utm_campaign: null}
          - {session_id: paid, visitor_id: device_paid, event_ts: "2026-09-01 00:01:00+00", session_entry_page_path: /, session_entry_referring_domain: www.google.com, session_entry_utm_source: google, session_entry_utm_medium: cpc, session_entry_utm_campaign: null}
          - {session_id: google_jobs, visitor_id: device_google_jobs, event_ts: "2026-09-01 00:02:00+00", session_entry_page_path: /, session_entry_referring_domain: $direct, session_entry_utm_source: google_jobs_apply, session_entry_utm_medium: organic, session_entry_utm_campaign: null}
          - {session_id: ai, visitor_id: device_ai, event_ts: "2026-09-01 00:03:00+00", session_entry_page_path: /, session_entry_referring_domain: gemini.google.com, session_entry_utm_source: null, session_entry_utm_medium: null, session_entry_utm_campaign: null}
          - {session_id: webmail, visitor_id: device_webmail, event_ts: "2026-09-01 00:04:00+00", session_entry_page_path: /, session_entry_referring_domain: mail.google.com, session_entry_utm_source: null, session_entry_utm_medium: null, session_entry_utm_campaign: null}
          - {session_id: search, visitor_id: device_search, event_ts: "2026-09-01 00:05:00+00", session_entry_page_path: /, session_entry_referring_domain: www.google.com, session_entry_utm_source: null, session_entry_utm_medium: null, session_entry_utm_campaign: null}
          - {session_id: internal, visitor_id: device_internal, event_ts: "2026-09-01 00:06:00+00", session_entry_page_path: /, session_entry_referring_domain: www.analyticsengineeringjobs.com, session_entry_utm_source: null, session_entry_utm_medium: null, session_entry_utm_campaign: null}
          - {session_id: direct, visitor_id: device_direct, event_ts: "2026-09-01 00:07:00+00", session_entry_page_path: /, session_entry_referring_domain: $direct, session_entry_utm_source: null, session_entry_utm_medium: null, session_entry_utm_campaign: null}
          - {session_id: referral, visitor_id: device_referral, event_ts: "2026-09-01 00:08:00+00", session_entry_page_path: /, session_entry_referring_domain: t.co, session_entry_utm_source: null, session_entry_utm_medium: null, session_entry_utm_campaign: null}
          - {session_id: null, visitor_id: device_no_session, event_ts: "2026-09-01 01:00:00+00", session_entry_page_path: /, session_entry_referring_domain: $direct, session_entry_utm_source: null, session_entry_utm_medium: null, session_entry_utm_campaign: null}
    expect:
      rows:
        - {web_session_id: 5da9375c84289105aab6cdd2d38f72cb, source_session_id: newsletter, source: posthog, visitor_id: device_newsletter, session_started_ts: "2026-09-01 00:00:00+00", session_entry_page_path: /, session_entry_referring_domain: $direct, session_entry_utm_source: newsletter, session_entry_utm_medium: email, session_entry_utm_campaign: null, channel: email}
        - {web_session_id: 47127c83addb4ea7209af049aa7ed399, source_session_id: paid, source: posthog, visitor_id: device_paid, session_started_ts: "2026-09-01 00:01:00+00", session_entry_page_path: /, session_entry_referring_domain: www.google.com, session_entry_utm_source: google, session_entry_utm_medium: cpc, session_entry_utm_campaign: null, channel: paid}
        - {web_session_id: 6254e249dd1db78827b41e158b79dfdb, source_session_id: google_jobs, source: posthog, visitor_id: device_google_jobs, session_started_ts: "2026-09-01 00:02:00+00", session_entry_page_path: /, session_entry_referring_domain: $direct, session_entry_utm_source: google_jobs_apply, session_entry_utm_medium: organic, session_entry_utm_campaign: null, channel: organic_search}
        - {web_session_id: ce524991008b1ee13539f9c1c3d26a0c, source_session_id: ai, source: posthog, visitor_id: device_ai, session_started_ts: "2026-09-01 00:03:00+00", session_entry_page_path: /, session_entry_referring_domain: gemini.google.com, session_entry_utm_source: null, session_entry_utm_medium: null, session_entry_utm_campaign: null, channel: ai_assistant}
        - {web_session_id: 681d1e812c58fdc60284300945561627, source_session_id: webmail, source: posthog, visitor_id: device_webmail, session_started_ts: "2026-09-01 00:04:00+00", session_entry_page_path: /, session_entry_referring_domain: mail.google.com, session_entry_utm_source: null, session_entry_utm_medium: null, session_entry_utm_campaign: null, channel: email}
        - {web_session_id: 285fe3e36149574edb27d7d7a28b7ddf, source_session_id: search, source: posthog, visitor_id: device_search, session_started_ts: "2026-09-01 00:05:00+00", session_entry_page_path: /, session_entry_referring_domain: www.google.com, session_entry_utm_source: null, session_entry_utm_medium: null, session_entry_utm_campaign: null, channel: organic_search}
        - {web_session_id: ac16f3651a9bab7db4b47abd47f255b5, source_session_id: internal, source: posthog, visitor_id: device_internal, session_started_ts: "2026-09-01 00:06:00+00", session_entry_page_path: /, session_entry_referring_domain: www.analyticsengineeringjobs.com, session_entry_utm_source: null, session_entry_utm_medium: null, session_entry_utm_campaign: null, channel: internal}
        - {web_session_id: ca3536ebc5701c19a5ea8f862978dde8, source_session_id: direct, source: posthog, visitor_id: device_direct, session_started_ts: "2026-09-01 00:07:00+00", session_entry_page_path: /, session_entry_referring_domain: $direct, session_entry_utm_source: null, session_entry_utm_medium: null, session_entry_utm_campaign: null, channel: direct}
        - {web_session_id: 11795594f59594de536c39d265d25489, source_session_id: referral, source: posthog, visitor_id: device_referral, session_started_ts: "2026-09-01 00:08:00+00", session_entry_page_path: /, session_entry_referring_domain: t.co, session_entry_utm_source: null, session_entry_utm_medium: null, session_entry_utm_campaign: null, channel: referral}
